---
- name: Create BuiltinTag example
  gather_facts: false
  hosts: localhost

  tasks:
    # Using node mode
    - name: Create tag using node mode allow_upsert=True
      opsmill.infrahub.create_node:
        kind: "BuiltinTag"
        data:
          name: "my-tag1"
          description: "This is my first tag description"

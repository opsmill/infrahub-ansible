---
- name: Create BuiltinTag example
  gather_facts: false
  hosts: localhost

  tasks:
    - name: Create tag using node mode
      opsmill.infrahub.node:
        kind: "BuiltinTag"
        data:
          name: "my-tag1"
          description: "This is my first tag description"

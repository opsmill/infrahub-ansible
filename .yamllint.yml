---
extends: default

ignore: |
  /.venv
  /examples
  /tests

rules:
  new-lines: disable
  line-length:
    max: 120
    allow-non-breakable-words: true
    allow-non-breakable-inline-mappings: false

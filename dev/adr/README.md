# Decision Records (ADRs)

MADR-format decision records. Name each file `nnnn-title.md`: a zero-padded 4-digit
sequence and a lowercase kebab-case title, no `adr-` prefix (e.g. `0001-two-plugin-patterns.md`).

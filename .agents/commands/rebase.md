---
description: Rebase a feature branch onto its latest base, resolving conflicts while preserving the intent of local changes.
argument-hint: Optional `push` to force-push and monitor CI after a successful rebase
---

## User Input

```text
$ARGUMENTS
```

Use the **rebase** skill (`.agents/skills/rebase/SKILL.md`) to handle this request. Follow it exactly, taking the user input above into account (if any).

---
description: Watch an open pull request's CI until green and fix failing checks.
argument-hint: Optional PR number or URL (defaults to the PR for the current branch)
---

## User Input

```text
$ARGUMENTS
```

Use the **monitoring-pull-requests** skill (`.agents/skills/monitoring-pull-requests/SKILL.md`) to handle this request. Follow it exactly, taking the user input above into account (if any).

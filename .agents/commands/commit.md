---
description: Stage and commit the current changes onto a safe working branch, enforcing branch discipline and optionally pushing upstream.
argument-hint: Optional `push` to push the branch upstream after committing.
---

## User Input

```text
$ARGUMENTS
```

Use the **commit** skill (`.agents/skills/commit/SKILL.md`) to handle this request. Follow it exactly, taking the user input above into account (if any).

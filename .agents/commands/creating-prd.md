---
description: Synthesise the current conversation context into a Product Requirements Document and publish it to GitHub.
argument-hint: Optional — extra instructions, scope hints, or an explicit issue number/URL to target
---

## User Input

```text
$ARGUMENTS
```

Use the **creating-prd** skill (`.agents/skills/creating-prd/SKILL.md`) to handle this request. Follow it exactly, taking the user input above into account (if any).

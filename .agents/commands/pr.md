---
description: Open a pull request, publishing the current branch as a PR.
argument-hint: Optional `commit` to stage, commit, and push uncommitted changes before opening the PR
---

## User Input

```text
$ARGUMENTS
```

Use the **pr** skill (`.agents/skills/pr/SKILL.md`) to handle this request. Follow it exactly, taking the user input above into account (if any).

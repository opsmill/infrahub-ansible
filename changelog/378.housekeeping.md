Type check `plugins/module_utils/infrahub_utils.py`, the collection's largest module. It was excluded from mypy, so a change confined to it reported success without being checked at all; the exclusion is gone and the errors it was hiding are fixed. `invoke lint` now runs mypy alongside ruff, matching CI's linter job.
